```mermaid
graph TD
    A[输入 n 和 k] --> B[初始化人数数组]
    B --> C[开始循环]
    C --> D{是否还有人}
    D -->|有| E[继续计数]
    D -->|没有| F[结束]
    E --> G{计数是否到 k}
    G -->|是| H[打印退出人的编号]
    G -->|否| E
    H --> I[标记此人退出]
    I --> J[更新剩余人数]
    J --> C
    F --> K[输出退出顺序]
